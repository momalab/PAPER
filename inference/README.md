# Privacy-Preserving Inference with PAPER

Describe what this directory is about.

# Dependencies

* Make $\ge$ 4.3: `sudo apt install make`

* CMake $\ge$ 3.22.1: `sudo apt install cmake`

* GNU C/C++17 compiler (or newer): `sudo apt install gcc g++`

* GMP library $\ge$ 6.2.1: `sudo apt install libgmp-dev`

* Git $\ge$ 2.34.1: `sudo apt install git`

* Python 3 with NumPY, ...
```
sudo apt install python3
...
```

* Microsoft SEAL library. Check dependencies in https://github.com/microsoft/seal and install with
```
cd third-party
bash install_seal.sh
```

# Compilation

...

# Run

...

